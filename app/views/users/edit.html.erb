<%= form_for @user do |f| %>
  <div class="form-group">
    <%= f.label :bio %>
    <%= f.text_area :bio %>
  </div>

  <div class="form-group">
    <%= f.label :email %>
    <%= f.email_field :email %>
  </div>

  <%= f.submit %>
<% end %>
